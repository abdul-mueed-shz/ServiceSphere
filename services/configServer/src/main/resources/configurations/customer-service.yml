server:
  port: 8090

logging:
  level:
    org.springframework: INFO

spring:
  data:
    mongodb:
      username: root
      password: root
      host: localhost
      port: 27017
      authentication-database: admin
      database: customer
